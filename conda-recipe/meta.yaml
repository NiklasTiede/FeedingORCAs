
{% set data = load_setup_py_data() %}

package:
  name: feedingORCAs
  version: {{ data['version'] }}

source:
  path: ..

build:
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
  run:
    - python
    # dependencies are defined in setup.py
    {% for dep in data['install_requires'] %}
    - {{ dep.lower() }}
    {% endfor %}

test:
  imports:
    - feedingORCAs
  source_files:
    - tests
  requires:
    - pytest
  commands:
    - pytest tests

about:
  home: TODO
  summary: interface with MongoDB to ORCA
